<SPConfig xmlns="urn:mace:shibboleth:3.0:native:sp:config"
    xmlns:conf="urn:mace:shibboleth:3.0:native:sp:config"
    xmlns:saml="urn:oasis:names:tc:SAML:2.0:assertion"
    xmlns:samlp="urn:oasis:names:tc:SAML:2.0:protocol"
    xmlns:md="urn:oasis:names:tc:SAML:2.0:metadata"
    xmlns:mdui="urn:oasis:names:tc:SAML:metadata:ui"
    clockSkew="180">

    <!-- Load plugins for Transform Attribute Resolver -->
    <OutOfProcess>
        <Extensions>
            <Library path="plugins.so" />
        </Extensions>
    </OutOfProcess>

    <!--
    By default, in-memory StorageService, ReplayCache, ArtifactMap, and SessionCache
    are used. See example-shibboleth2.xml for samples of explicitly configuring them.
    -->

    <!--
    To customize behavior for specific resources on Apache, and to link vhosts or
    resources to ApplicationOverride settings below, use web server options/commands.
    See https://wiki.shibboleth.net/confluence/display/SHIB2/NativeSPConfigurationElements for help.

    For examples with the RequestMap XML syntax instead, see the example-shibboleth2.xml
    file, and the https://wiki.shibboleth.net/confluence/display/SHIB2/NativeSPRequestMapHowTo topic.
    -->

    <RequestMapper type="XML">
        <RequestMap>
            <Host name="jupyter.rwth-aachen.de"
                  requireSession="false"
                  authType="shibboleth"
                  redirectToSSL="443">
                <Path name="/" />
            </Host>
        </RequestMap>
    </RequestMapper>

    <!-- The ApplicationDefaults element is where most of Shibboleth's SAML bits are defined. -->
    <ApplicationDefaults id="default" policyId="default" entityID="https://jupyter.rwth-aachen.de/shibboleth"
                         REMOTE_USER="persistent-hash">

        <!--
        Controls session lifetimes, address checks, cookie handling, and the protocol handlers.
        You MUST supply an effectively unique handlerURL value for each of your applications.
        The value defaults to /Shibboleth.sso, and should be a relative path, with the SP computing
        a relative value based on the virtual host. Using handlerSSL="true", the default, will force
        the protocol to be https. You should also set cookieProps to "https" for SSL-only sites.
        Note that while we default checkAddress to "false", this has a negative impact on the
        security of your site. Stealing sessions via cookie theft is much easier with this disabled.
        -->
        <Sessions lifetime="28800" timeout="3600" relayState="ss:mem"
                  checkAddress="false" consistentAddress="true"
                  handlerSSL="true" cookieProps="https"
                  redirectLimit="exact">

            <!--
            Configures SSO for a default IdP. To allow for >1 IdP, remove
            entityID property and adjust discoveryURL to point to discovery service.
            (Set discoveryProtocol to "WAYF" for legacy Shibboleth WAYF support.)
            You can also override entityID on /Login query string, or in RequestMap/htaccess.
            -->
            <SSO entityID="https://login.rz.rwth-aachen.de/shibboleth">
              SAML2
            </SSO>

            <!-- SAML and local-only logout. -->
            <Logout>SAML2 Local</Logout>

            <!-- Extension service that generates "approximate" metadata based on SP configuration. -->
            <Handler type="MetadataGenerator" Location="/Metadata" signing="false">
                <mdui:UIInfo>
                    <mdui:DisplayName xml:lang="de">RWTHjupyer</mdui:DisplayName>
                    <mdui:DisplayName xml:lang="en">RWTHjupyer</mdui:DisplayName>
                    <mdui:Description xml:lang="de">JupyterLab interaktive Umgebung</mdui:Description>
                    <mdui:Description xml:lang="en">JupyterLab computational environment</mdui:Description>
                </mdui:UIInfo>
                <md:Organization>
                    <md:OrganizationName xml:lang="de">IT Center, RWTH Aachen University</md:OrganizationName>
                    <md:OrganizationName xml:lang="en">IT Center, RWTH Aachen University</md:OrganizationName>
                    <md:OrganizationDisplayName xml:lang="de">IT Center, RWTH Aachen University</md:OrganizationDisplayName>
                    <md:OrganizationDisplayName xml:lang="en">IT Center, RWTH Aachen University</md:OrganizationDisplayName>
                    <md:OrganizationURL xml:lang="de">https://itc.rwth-aachen.de</md:OrganizationURL>
                    <md:OrganizationURL xml:lang="en">https://itc.rwth-aachen.de</md:OrganizationURL>
                </md:Organization>
                <md:ContactPerson contactType="support">
                    <md:GivenName>Support</md:GivenName>
                    <md:SurName>Adresse</md:SurName>
                    <md:EmailAddress>servicedesk@itc.rwth-aachen.de</md:EmailAddress>
                </md:ContactPerson>
                <md:ContactPerson contactType="technical">
                    <md:GivenName>Technischer</md:GivenName>
                    <md:SurName>Ansprechpartner</md:SurName>
                    <md:EmailAddress>jupyter-admin@lists.rwth-aachen.de</md:EmailAddress>
                </md:ContactPerson>
                <md:ContactPerson contactType="administrative">
                    <md:GivenName>Adminstrativer</md:GivenName>
                    <md:SurName>Ansprechpartner</md:SurName>
                    <md:EmailAddress>jupyter-admin@lists.rwth-aachen.de</md:EmailAddress>
                </md:ContactPerson>
            </Handler>

            <!-- Status reporting service. -->
            <Handler type="Status" Location="/Status"/>

            <!-- Session diagnostic service. -->
            <Handler type="Session" Location="/Session" showAttributeValues="true"/>

            <!-- JSON feed of discovery information. -->
            <Handler type="DiscoveryFeed" Location="/DiscoFeed"/>
        </Sessions>

        <!--
        Allows overriding of error template information/filenames. You can
        also add attributes with values that can be plugged into the templates.
        -->
        <Errors supportContact="servicedesk@itc.rwth-aachen.de"
            helpLocation="/about.html"
            styleSheet="/shibboleth-sp/main.css"/>

        <!-- RWTH Aachen Metadaten -->
        <MetadataProvider type="XML" url="https://sso.rwth-aachen.de/metadata/rwth.metadata.xml" backingFilePath="rwth.metadata.xml" maxRefreshDelay="7200">
            <MetadataFilter type="Signature" certificate="/etc/cert-sso-2019/tls.crt"/>
        </MetadataProvider>

        <!-- Map to extract attributes from SAML assertions. -->
        <AttributeExtractor type="XML" validate="true" reloadChanges="false" path="attribute-map-rwth.xml"/>
        <AttributeExtractor type="XML" validate="true" reloadChanges="false" path="attribute-map.xml"/>

        <AttributeResolver type="Transform" source="persistent-id">
            <Regex match="^(.*)!(.*)!(.{8})(.*)$" dest="persistent-hash-mixed-case">$3</Regex>
        </AttributeResolver>

        <AttributeResolver type="LowerCase" source="persistent-hash-mixed-case" dest="persistent-hash" />

        <!-- Use a SAML query if no attributes are supplied during SSO. -->
        <AttributeResolver type="Query" subjectMatch="true"/>

        <!-- Default filtering policy for recognized attributes, lets other data pass. -->
        <AttributeFilter type="XML" validate="true" path="attribute-policy.xml"/>

        <!-- Chaining credential resolver for certificate rollover
             See: https://www.switch.ch/aai/guides/sp/certificate-rollover/
        -->
        <CredentialResolver type="Chaining">
             <!-- Active certificate -->
             <CredentialResolver type="File"
                                 key="/etc/cert-jupyter-2018/tls.key"
                                 certificate="/etc/cert-jupyter-2018/tls.crt"/>
             <!-- Additional new certificate -->
             <CredentialResolver type="File"
                                 key="/etc/cert-jupyter-2021/tls.key"
                                 certificate="/etc/cert-jupyter-2021/tls.crt"/>
        </CredentialResolver>

        <!--
        The default settings can be overridden by creating ApplicationOverride elements (see
        the https://wiki.shibboleth.net/confluence/display/SHIB2/NativeSPApplicationOverride topic).
        Resource requests are mapped by web server commands, or the RequestMapper, to an
        applicationId setting.

        Example of a second application (for a second vhost) that has a different entityID.
        Resources on the vhost would map to an applicationId of "admin":
        -->

        <!--
        <ApplicationOverride id="admin" entityID="https://admin.example.org/shibboleth"/>
        -->
    </ApplicationDefaults>

    <!-- Policies that determine how to process and authenticate runtime messages. -->
    <SecurityPolicyProvider type="XML" validate="true" path="security-policy.xml"/>

    <!-- Low-level configuration about protocols and bindings available for use. -->
    <ProtocolProvider type="XML" validate="true" reloadChanges="false" path="protocols.xml"/>
</SPConfig>
